syntax = "v1"

import (

)

info(
	title: "etcd 配置中台"
	desc: "实现etcd配置可视化、以及版本管理"
	author: "lime"
	email: "1280291001@qq.com"
	version: "1.0"
)

@server(
	prefix: api/v1/configure
)
service configure-api{
	@doc "服务检查"
	@handler CheckHealth
	post /check_health
}